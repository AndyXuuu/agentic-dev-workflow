version: 1
project: your-project-name
type: repo-nav-index

usage:
  priority: first-stop
  instruction: >-
    Read this index before broad repo search. Use it to narrow scope, then
    fall back to read/grep/glob/LSP when needed.

entrypoints:
  docs: .repo-nav/docs.yaml
  modules: .repo-nav/modules.yaml
  workflows: .repo-nav/workflows.yaml

project_entrypoints:
  root-readme: README.md

unknowns:
  - list unresolved high-level navigation gaps here
