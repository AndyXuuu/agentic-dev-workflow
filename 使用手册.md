# Agentic Development Workflow 使用手册

> 面向 AI Coding Agent 的项目无关软件工程治理与交付框架。

本手册用于解释和示例，不是第二套规范源。强制行为以当前仓库 `AGENTS.md` 和被明确调用的
Skill 为准；本手册与其不一致时应修正本手册，不能用示例覆盖权威规则。

这不是一组零散的提示词或角色配置，而是一套把 Agent 纳入正式软件交付体系的工程框架。它借鉴成熟技术组织在需求评审、架构治理、Owner 边界、变更控制、质量门禁、行为测试、代码审查和发布恢复方面的共性实践，让 Agent 从“能够生成代码”升级为“能够在明确责任、证据和安全边界下完成交付”的工程执行者。

框架不复刻某一家公司的内部制度，也不声称代表某种行业认证。它提炼大规模软件团队长期验证过的工程原则，并把这些原则转化为 Agent 可以稳定执行、团队可以检查、项目可以独立继承的规则、Skills、模板和验证入口。

它追求七个核心结果：

- **治理明确**：先识别风险和责任边界，再选择与风险匹配的交付流程。
- **判断有据**：把目标与技术前提分开，先核验现状再形成独立建议，保留“不修改”这一正确选项。
- **过程可追溯**：需求、设计、实现、测试与交付证据形成连续链路。
- **架构可理解**：每个行为都有可搜索的 Owner、显式依赖和局部验证路径。
- **质量可验证**：测试验证预期行为，不迎合当前实现，不用修改断言掩盖缺陷。
- **变更可恢复**：保护用户工作，显式报告风险，并在必要时提供回滚或恢复方案。
- **能力可复用**：全局层沉淀项目无关的工程方法，具体项目继续拥有技术栈、契约、命令和业务规则。

这套全局工程环境独立于产品仓库维护，目录是：

```text
$HOME/Documents/codex/agentic-dev-workflow
```

## 一、核心原则

不要让 Agent 在风险未分类时直接进入写代码模式。明确、局部、低风险的小改动
使用 `AGENTS.md` 定义的 Fast Path；不满足全部条件时再进入标准流程。

风险边界出现时只是退出 Fast Path，并不自动扩大主任务。安全控制先固定保护资产、威胁或
故障、攻击者能力、信任边界和可观察保证；已规定的安全基线继续执行，但不得把防误操作
自行升级为防凭证失陷、内部恶意或供应链攻击。完整规则以 `AGENTS.md` 的
`Security Goal and Complexity Budget` 为唯一权威来源。

标准流程是：

```text
需求分析 -> 架构设计 -> 开发实现 -> 测试回归 -> 交付 Review
```

明确、局部且不改变验收、Owner、契约、数据、权限、迁移或交付风险的变化可以直接吸收；
material 需求或架构变化才暂停一次，说明差异、影响和修订计划。完整规则以 `AGENTS.md`
的 `Change Control` 为唯一权威来源。

### Fast Path

文档、格式、注释、非敏感本地配置，以及验收明确且局限于现有 Owner 的小改动，
在不涉及契约、数据、权限、安全、依赖、生成、部署或跨模块边界时执行：

```text
确认 Owner 和最小范围 -> 修改 -> 最小相关验证 -> 简短交付
```

Fast Path 在加载可选的生命周期或工程领域 Skill 前判定，不因文件属于前端或后端就自动加载
`$ax-dev`、`$ax-frontend` 或 `$ax-backend`。它不生成 PRD 或完整设计报告，也不默认运行无关的全量测试、Lint 和构建。
详细准入和升级条件以 `AGENTS.md` 的 `Risk-Tiered Workflow` 为唯一权威来源。

### 决策协作模型

Vibe Coding 中，用户通常把注意力放在目标、优先级和取舍，而不是逐项核对实现细节。
因此 Agent 不能把用户说出的技术原因或修改办法直接翻译成代码。双方职责是：

| 参与者 | 负责内容 |
|---|---|
| 用户 | 目标、业务优先级、审美偏好、风险取舍、明确约束和最终决策 |
| Agent | 调查当前事实、区分现状与意图、评估方案与代价、给出独立建议、实施并验证结果 |

用户说“使用 ApexCharts”可以是明确技术约束；“ApexCharts 已经开源且当前项目正在使用”
则包含可核验事实。用户说“页面空间利用率低”描述的是期望改善的问题；“把全部字号缩小”
只是候选方案。Agent 应先检查真实页面、Token 和密度来源，再建议保持现状、局部修正或采用
该方案，不能在取得证据前用“你说得对”代替工程判断。

改进已有实现时，Agent 先记录与目标相关的基线和成功信号，修改后使用同一信号比较。
测试通过只说明已保护的契约仍成立，不能单独证明结构、性能、可用性或视觉效果变得更好。
完整执行规则以 `AGENTS.md` 的 `Evidence and Engineering Judgment` 为唯一权威来源。

假设只能是短生命周期的工作假说，不能逐步沉淀成 Agent 的“项目事实”。当它开始影响
实现、测试预期、权威文档、归档或交付时，必须立即进入三种终态之一：用源码、配置、
运行状态、合同或测试证据证实为事实；由用户明确决定为目标或约束；或者退出并清理依赖
它的计划、测试、文档与代码。非阻断未知可以保留，但只能写成不预设答案的待确认问题或
证据缺口。这样既不会因所有未知暂停探索，也不会让 OpenSpec、计划或长期文档中的猜测
持续带偏后续 Agent。

### 全局与项目边界

```text
全局 AGENTS      -> 项目无关的工程门禁与安全规则
生命周期 Skill   -> 需求、架构、开发、测试、Review 阶段
工程领域 Skill   -> 可迁移的前端、后端与结构质量规范
仓库操作 Skill   -> Git 操作和项目接入流程
外部集成 Skill   -> TAPD 等外部工具的访问流程
项目 AGENTS      -> 项目技术栈、命令、强约束、权威来源和独立运行的最小门禁
项目适配 Skill   -> 把全局流程导航到项目 Owner，不复制全局规范
```

`$HOME/Documents/codex/agentic-dev-workflow` 的全局规则和 Skills 禁止写入具体项目路径、框架选择、接口地址、部署拓扑或业务规则。跨仓契约必须明确唯一源契约，生成投影、同步副本和消费者客户端都保持派生关系。

## 二、模型分工

| 角色 | 模型 | 用途 |
|---|---|---|
| 架构 / Review | `gpt-5.6-sol` | 高风险判断、架构设计、最终审查 |
| 开发 | `gpt-5.6-terra` | 日常实现、普通 Bug、重构落地 |
| 测试 / 文档 | `gpt-5.6-luna` | 测试用例、回归场景、文档、重复任务 |

配置文件：

```text
~/.codex/arch.config.toml    -> gpt-5.6-sol
~/.codex/dev.config.toml     -> gpt-5.6-terra
~/.codex/test.config.toml    -> gpt-5.6-luna
~/.codex/review.config.toml  -> gpt-5.6-sol
```

## 三、Skill 分类与说明

当前工程 Skills 按职责分为 `lifecycle`、`disciplines`、`repository` 和 `integrations`。物理目录由 `skills/catalog.tsv` 统一登记，用户调用名保持扁平且稳定。全部 Skill 使用小写连字符名称；安装脚本会删除旧下划线软链接。

| 模块 | Skill | 菜单说明 | 什么时候用 |
|---|---|---|---|
| `lifecycle` | `$ax-pipeline` | 全流程：需求 -> 架构 -> 开发 -> 测试 -> 交付 | 复杂功能、Bug 修复、重构、上线前完整流程 |
| `lifecycle` | `$ax-prd` | 需求 / PRD | 需求不清楚、需要拆范围和验收标准 |
| `lifecycle` | `$ax-arch` | 架构设计 | 开发前设计模块边界、数据流、接口、迁移 |
| `lifecycle` | `$ax-dev` | 开发实现 | 已有 PRD/设计后，按方案写代码 |
| `lifecycle` | `$ax-test` | 测试 / 回归测试 | 补行为测试、回归测试、边界用例 |
| `lifecycle` | `$ax-review` | 交付前 Review | PR/上线前检查风险、测试、回滚方案 |
| `disciplines` | `$ax-frontend` | 通用前端工程流程 | 前端实现、设计系统适配、响应式与可访问性验证 |
| `disciplines` | `$ax-backend` | 通用后端工程流程 | API、领域边界、数据一致性、并发、安全与迁移验证 |
| `disciplines` | `$ax-structure-review` | 代码结构审核 | 检查职责边界、依赖方向、重复逻辑、抽象与复杂度 |
| `repository` | `$git-workflow` | Git 提交与同步 | 安全执行本地提交、远程同步、分叉判断和冲突停机 |
| `repository` | `$ax-sdd` | 实验性当前系统 SDD | 按任务快速定位当前规格，也可封装无源码 Builder 输入 |
| `repository` | `$ax-project-bootstrap` | 新项目脚手架引导 | 为全新项目选择并完整复制已登记脚手架，再建立项目基线 |
| `repository` | `$ax-project-adapter` | 项目适配层生成 | 根据项目架构、Owner、文档和命令生成精简项目 Skill |
| `integrations` | `$tapd-query` | TAPD 只读查询 | 按工作区、迭代、人员或状态查询需求和 Bug |

## 四、安装和刷新

安装：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/install.sh
```

安装后会创建软链接：

```text
~/.agents/skills/*
~/.codex/arch.config.toml
~/.codex/dev.config.toml
~/.codex/test.config.toml
~/.codex/review.config.toml
~/.codex/AGENTS.agentic-dev-workflow.md
```

注意：

```text
不会覆盖 ~/.codex/AGENTS.md
```

如果你希望这套工程规则成为全局默认规则，需要把：

```text
~/.codex/AGENTS.agentic-dev-workflow.md
```

合并到：

```text
~/.codex/AGENTS.md
```

推荐使用项目提供的脚本建立软链接，这样修改当前项目的 `AGENTS.md`
就会同步到全局：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/apply-global-agent.sh
```

脚本行为：

```text
~/.codex/AGENTS.md -> $HOME/Documents/codex/agentic-dev-workflow/AGENTS.md
```

- 目标为空文件时，直接替换为软链接
- 目标已是正确软链接时，重复执行不会修改任何内容
- 目标存在非空内容或其他软链接时，先备份为
  `~/.codex/AGENTS.md.backup.<时间戳>`

之后只需编辑：

```text
$HOME/Documents/codex/agentic-dev-workflow/AGENTS.md
```

如需回滚，删除 `~/.codex/AGENTS.md` 软链接，再将脚本输出的备份文件
改回 `~/.codex/AGENTS.md` 即可。

刷新方式：

```text
完全退出 Codex / ChatGPT App
重新打开
```

只新开会话不一定会刷新 skill 菜单缓存。

### 安装 TAPD MCP

首先安装 Skills/Profile 软链接，然后在交互式终端执行：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/install-tapd-mcp.sh
```

如果 `~/.zprofile` 中已定义 `TAPD_KEY`，脚本会配置 MCP 在启动时动态读取，
不会把令牌再复制到 Codex 配置。否则脚本会隐藏输入 TAPD 个人访问令牌，
并仅保存到 Codex 用户 MCP 配置。令牌不会写入当前 Git 仓库。
安装后完全退出并重新打开 Codex / ChatGPT。

查询示例：

```text
$tapd-query 获取 workspace 123456 的需求和 Bug 列表。
$tapd-query 查询某迭代中由张三负责的需求，并汇总未关闭 Bug。
```

Git 操作示例：

```text
$git-workflow 检查当前改动并创建准确的本地提交。
$git-workflow 安全同步当前分支与远程 upstream。
```

## 五、常用启动方式

进入项目目录：

```bash
cd /path/to/project
```

按角色启动：

```bash
codex -p arch --cd /path/to/project
codex -p dev --cd /path/to/project
codex -p test --cd /path/to/project
codex -p review --cd /path/to/project
```

在 Codex 对话里直接调用：

```text
$ax-pipeline 处理这个需求，先不要写代码，按完整流程输出。
```

## 六、推荐工作流

### 实验：当前系统 SDD

当项目明确希望让 Agent 更快理解项目与当前任务，并验证“没有原源码时能否重建行为等价系统”，使用：

```text
$ax-sdd 为当前项目初始化实验性 current-system SDD，并先运行结构门禁。
```

它是 `$ax-pipeline` 的支持能力，不是第二套需求、设计、开发、测试流程。长期 SDD 只保存
完整当前规格、机器契约、必要资产、Requirement → Oracle 关系以及非规范任务路由；当前任务来自
本次请求，不保存为 SDD Artifact。Proposal、tasks、探索过程、
被否决方案和历史快照交给 Git、PR/Issue 或独立审计存储，不进入 Builder 默认搜索空间。
迁移期间 SDD 必须保持 draft，边界内既有 canonical Owner 继续有效；切换为 current 前，要把旧
权威文档迁入唯一登记 Artifact，并将原位置删除、链接化或改成带漂移检查的生成投影，不能长期
维护同一边界内 `docs/` 与 SDD 两份手写规范。边界外项目规则和 Owner 仍按项目定义生效。

命令：

```bash
python3 skills/repository/ax-sdd/scripts/sdd.py init /path/to/project/sdd \
  --system-id example-system --name "Example System"
python3 skills/repository/ax-sdd/scripts/sdd.py context /path/to/project/sdd \
  --query "修改 API 权限"
python3 skills/repository/ax-sdd/scripts/sdd.py validate /path/to/project/sdd --level structure
python3 skills/repository/ax-sdd/scripts/sdd.py validate /path/to/project/sdd
python3 skills/repository/ax-sdd/scripts/sdd.py bundle \
  /path/to/project/sdd /path/to/output/example-system-sdd-0.1.0.zip
```

日常先用 `context` 获取最小阅读切片，不默认读取全部 SDD。`structure` 允许显式草稿和 TODO，只证明目录、登记、引用与可追踪结构正确；默认完整门禁要求
全部内容已收敛为 current。Bundle 文件名必须匹配 `<system-id>-sdd-<manifest-version>.zip`；它只包含 manifest 登记内容，并不包含原源码或仓库其他文档。
两类静态门禁与 bundle 成功都不能替代周期性的局部/全量清洁重建；只有独立 Builder 无权读取
原源码和历史、且独立 Evaluator 验收通过，才可报告清洁重建成功。

项目及附近的 Agent 默认搜索空间只保留一个 current bundle。迭代生成放在临时目录；已被独立
实验、发布、签名或外部引用的旧 bundle 移到搜索空间之外的审计存储，其余中间 bundle 删除。
普通规格演进由 Git 保存，不用一组旧 ZIP 充当历史。

### 0. 新建项目或应用包

目标应用尚不存在时，先生成框架基线，再规划业务工作流：

```text
$ax-project-bootstrap 为这个新 Admin 项目选择已登记脚手架并完整生成到目标目录。
```

Bootstrap 先检查应用类型、技术栈硬约束、许可证和目标路径，再使用官方生成器完整复制
源码、锁文件、`AGENTS.md`、设计契约、项目 Adapter 和验证入口。生成器已经通过的聚合
验证不在同一份未修改输出上重复执行。生成成功后才使用 `$ax-project-adapter` 校准项目
导航，并由 `$ax-pipeline` 从实际 Owner 规划业务改动。

目标目录已经存在时不得覆盖或手工拼接脚手架；这种情况按迁移或架构任务处理。

### 1. 新功能

```text
$ax-pipeline 我们要做「用户钱包冻结余额」。请按需求分析、架构设计、开发计划、测试计划、交付检查输出，先不要写代码。
```

如果需求清楚，可以分阶段，但仍只有一个 primary workflow；领域 Skill 在开发阶段提供
专项决策，不成为额外生命周期阶段：

```text
$ax-prd 输出 PRD 和验收标准
$ax-arch 基于 PRD 输出架构方案
$ax-dev 按设计实现，并按需组合 $ax-frontend / $ax-backend 的专项边界
$ax-test 补充行为测试和回归测试
$ax-structure-review 审核代码结构（明确需要结构审计时）
$ax-review 做交付前检查
```

### 生成项目适配层

进入目标仓库后执行：

```text
$ax-project-adapter 检查当前项目，并根据实际架构生成或精简项目适配 Skill。
```

生成结果默认位于 `.agents/skills/<adapter-name>/`，只保存项目 Owner、权威文档、
生成/只读边界和验证入口。项目 `AGENTS.md` 同时保留需求范围、Owner/复用、行为验证
和交付报告的最小闭环，保证仓库脱离个人全局环境后仍能工作；安装了全局规则与
`ax-*` Skill 时使用更完整门禁，项目内不复制整套领域规范。

目标项目已经存在 `AGENTS.md` 时，生成器必须先完整读取并保留现有内容，只把缺失的
最小门禁合并到对应章节。禁止用模板整文件覆盖，也不重排、改写或删除无关规则；
发现现有规则与模板冲突时先报告；在当前运行环境的指令优先级内，项目现有约束相对模板
保持权威，等待确认后再处理。

全局工程仓库不保存项目绝对路径或适配器同步状态。默认处理当前打开的目标仓库或用户
明确提供的路径；“全部注册项目”“全部受控项目”只读取机器本地权威清单
`$HOME/.codex/project-registry.yaml` 中 `sync_rules: true` 的条目，不扫描父目录推断范围。
只有用户明确要求发现候选仓库时才检查其给出的有界父目录；发现不等于注册或同步授权。

### 2. Bug 修复

```text
$ax-pipeline 有个 bug：用户连续点击充值按钮会生成多笔订单。请先分析复现方式、影响范围、回归测试，再给修复方案。
```

先保留能够证明 Bug 的修复前失败证据，再实现修复并重复同一检查。只有满足
`AGENTS.md` Test Admission Gate 的稳定行为、契约、不变量或已知回归才保留为永久自动化
测试；否则使用聚焦的修复前后证据，不为满足形式制造测试。

### 3. 架构设计

```text
$ax-arch 冻结余额不能只放在 wallet 表字段里，需要改成 wallet_reservations 明细表。请分析模块边界、数据流、幂等、迁移和回滚方案。
```

### 4. 只做开发

适用于 PRD 和设计已经确认的情况：

```text
$ax-dev 根据已确认的 DESIGN.md 实现冻结余额接口。开始前先说明会复用哪些模块、改哪些文件、怎么测试。
```

### 5. 只做测试

```text
$ax-test 针对支付回调重复到达导致重复入账的问题，设计回归测试。测试必须验证业务行为，不能照抄当前实现。
```

### 6. 交付前检查

```text
$ax-review 本次改动包含钱包冻结、释放冻结、支付预授权。请检查需求匹配、架构风险、测试覆盖、上线风险和回滚方案。
```

## 七、需求变更怎么处理

如果开发过程中需求变了，先按 `AGENTS.md` Change Control 判断变化是否 material。明确、
局部且不改变验收、Owner、契约、数据、权限、迁移或交付风险的变化由当前 primary
workflow 直接吸收；material 变化才暂停一次做影响分析。

用：

```text
$ax-pipeline 当前任务开发中需求发生变化：原来只支持全额释放冻结，现在要支持部分释放。请暂停实现，先做变更影响分析，更新 PRD、架构和测试计划。
```

material 变化的最小影响说明包含：

```text
1. 需求或设计差异
2. 变化后的验收标准与受影响 Owner/契约/数据/权限/迁移/风险
3. 已有计划、测试和代码哪些继续有效，哪些需要修改或清理
4. 修订后的下一步，以及仍需用户决定或批准的事项
```

## 八、架构变更怎么处理

如果发现原设计不合理：

```text
$ax-arch 架构变更：原方案把冻结余额做成 wallet 字段，现在改成 reservation 明细表。请对比旧设计和新设计，列出影响文件、迁移方案、回归测试和回滚方案。
```

要求：

```text
先解释为什么旧设计不够
再给新设计
最后给迁移和测试计划
```

## 九、质量红线

以下情况不应该直接进入开发：

```text
非 Fast Path 需求没有验收标准
影响数据模型但没有迁移方案
影响支付/钱包/权限/计费但没有风险分析
没有搜索已有类似逻辑
同一业务规则准备复制到多个地方
Bug 修复没有修复前后同一失败证据；需要永久回归测试时未通过 Test Admission Gate
测试只是照着当前实现写断言
```

如果出现这些情况，应该回到：

```text
$ax-prd
$ax-arch
$ax-test
```

## 十、常用 Prompt 模板

### 完整需求

```text
$ax-pipeline 需求：<写需求>。请先不要写代码，按需求分析、架构设计、开发计划、测试计划、交付检查输出。
```

### 需求变更

```text
$ax-pipeline 需求变更：原需求是 <原需求>，现在改成 <新需求>。请暂停开发，做变更影响分析，并更新 PRD/设计/测试计划。
```

### 架构变更

```text
$ax-arch 架构变更：当前设计是 <旧设计>，现在需要改成 <新设计>。请分析原因、影响范围、迁移、测试和回滚。
```

### Bug 修复

```text
$ax-pipeline Bug：<描述问题>。请先给复现路径、影响范围、回归测试，再给修复方案。
```

### 测试补强

```text
$ax-test 针对 <功能/bug> 设计行为测试和回归验证，只选择本次变化实际影响的 happy path、异常输入、边界、权限/状态和邻近回归风险；永久测试必须通过 Test Admission Gate。
```

### 交付检查

```text
$ax-structure-review 审核本次改动的职责边界、依赖方向、重复逻辑和结构复杂度。
$ax-review 请对本次改动做交付前 Review：需求匹配、架构质量、重复逻辑、测试覆盖、上线风险、回滚方案。
```

## 十一、卸载

删除本项目安装的 Skill/Profile 链接，以及精确指向本仓库 `AGENTS.md` 的全局链接；不删除
本目录，也不删除已有 `AGENTS.md.backup.*` 备份。指向其他来源的全局链接会被保留：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/uninstall.sh
```

## 十二、维护建议

当你发现 Agent 反复犯同一个错误时，不要只在对话里纠正。应该把规则沉淀到：

```text
$HOME/Documents/codex/agentic-dev-workflow/AGENTS.md
```

或者对应 Skill：

```text
$HOME/Documents/codex/agentic-dev-workflow/skills/<module>/<skill-name>/SKILL.md
```

修改后运行：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/validate-skills.sh
$HOME/Documents/codex/agentic-dev-workflow/bin/validate-scaffolds.sh --catalog
$HOME/Documents/codex/agentic-dev-workflow/bin/install.sh
```

修改脚手架源或生成器时，明确接受依赖安装和临时磁盘开销后，再运行：

```bash
$HOME/Documents/codex/agentic-dev-workflow/bin/validate-scaffolds.sh --smoke
```

然后重启 Codex。
